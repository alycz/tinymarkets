{
  "name": "jet-prediction-market",
  "version": "0.1.0",
  "private": true,
  "description": "Short-duration (2-minute) BTC binary prediction market — local demo. One canonical YES order book + signed positions; VENUE_WEIGHTED_TWAP_V1 oracle.",
  "packageManager": "pnpm@9.12.0",
  "engines": { "node": ">=20" },
  "scripts": {
    "typecheck": "pnpm -r --if-present typecheck",
    "test": "pnpm -r --if-present test",
    "build": "pnpm -r --if-present build",
    "dev": "concurrently -k -n api,web -c cyan,green \"pnpm --filter @jet/api dev\" \"pnpm --filter @jet/web dev\"",
    "bots": "pnpm dev:bots",
    "dev:bots": "concurrently -k -n mm,takers -c magenta,yellow \"pnpm --filter @jet/bots dev\" \"pnpm --filter @jet/bots dev:takers\"",
    "demo": "concurrently -k -n api,web,mm,takers -c cyan,green,magenta,yellow \"pnpm --filter @jet/api dev\" \"pnpm --filter @jet/web dev\" \"pnpm --filter @jet/bots dev\" \"pnpm --filter @jet/bots dev:takers\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.6.0",
    "vitest": "^2.0.0"
  }
}
