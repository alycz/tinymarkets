{
  "name": "@jet/market-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Market lifecycle, signed-position ledger, collateral, settlement.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": { "typecheck": "tsc --noEmit", "test": "vitest run" },
  "dependencies": {
    "@jet/shared": "workspace:*",
    "@jet/config": "workspace:*"
  },
  "devDependencies": { "typescript": "^5.6.0", "vitest": "^2.0.0" }
}
