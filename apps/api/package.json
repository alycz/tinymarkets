{
  "name": "@jet/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Fastify REST + WebSocket server. Server-authoritative state.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": { "typecheck": "tsc --noEmit" },
  "dependencies": {
    "@jet/shared": "workspace:*",
    "@jet/config": "workspace:*"
  },
  "devDependencies": { "typescript": "^5.6.0" }
}
